DESCRIPTION
===========
Ping program that looks at the OS it is running on and then in a multi threaded method it will then ping through all of the hosts in a file named hosts.txt. The output of those pings will be printed out to the screen for your review.
